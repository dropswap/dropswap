{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn wsrun -a test",
    "dev": "yarn wsrun -ae dev",
    "build": "yarn wsrun -t build"
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
