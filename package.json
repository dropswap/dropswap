{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn wsrun -a -trp @dropswap/dsdk -c test",
    "dev": "yarn wsrun -ae dev",
    "build": "yarn wsrun -t build",
    "build:interface": "yarn wsrun -trp @dropswap/interface -c build"
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
