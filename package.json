{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn wsrun -am -trp @dropswap/dsdk -c test",
    "dev": "yarn wsrun -aem dev",
    "build": "yarn wsrun -tm build",
    "generate": "yarn wsrun -m generate",
    "build:interface": "yarn wsrun -trp @dropswap/interface -c build"
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
