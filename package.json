{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn lerna run --stream test",
    "dev": "yarn lerna run --stream dev",
    "build": "yarn lerna run --stream build"
  }
}
